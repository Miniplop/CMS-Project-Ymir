/*App.Views.CategorieView = Backbone.View.extend({
    template: _.template($('#categorie-template').html()),

    render: function() {
        console.log(JSON.stringify(this.model.get('widgets')));
        var html = this.template(this.model.toJSON(), {widgets:this.model.get('widgets')});
        this.$el.html(html);
        return this;
    }
});*/